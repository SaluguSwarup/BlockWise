# Integration log

The integrator's record of what shipped, what became available, and which fixture to retire.
Newest entry first. See `workflow.md` for how this is used.

---

## Night 0 — Pre-Day-1 (R1–R8)

**Shipped:** the whole foundation this log exists to support.

- **R1** — ownership boundaries recorded in `docs/ownership.md`; `backend/modules/{team-a,team-b,team-c}/`
  and `src/dev/{team-a,team-c}/` created as each team's footholds.
- **R2** — `packages/contracts/` frozen: response envelope, record shapes, `PlanningInput → PlanningOutput`,
  `ScoringInput → ScoringOutput`, date/time/id conventions.
- **R3** — `src/mocks/*.json` generated by `scripts/build-fixtures.mjs` from today's prototype data and a
  real run of the extracted engine. `plan.json` is the strict canned fixture — Team B does not run the
  engine as a fallback.
- **R4** — the prototype's planning logic extracted to `packages/engine/` (`@blockwise/engine`), split into
  a frozen scoring interface (deterministic placeholder scorer) and an optimiser that never computes or
  overrides a score. `src/lib/planningEngine.js` deleted from the frontend.
- **R5** — `backend/server.js` rewritten as an integrator-owned shell that auto-mounts
  `backend/modules/*/index.js`; the auth placeholder (`backend/lib/auth.js`) is in its final shape.
- **R6** — `/dev/data` (Team A) and `/dev/planner` (Team C) registered behind a dev-only flag; confirmed
  absent from a production build (`npm run build` + grep `dist/`).
- **R7** — Vitest installed as the one test runner (`node` project for `packages/`/`backend/`, `jsdom` for
  `src/`); CI added (`.github/workflows/ci.yml`); `pg` added to `backend/` for Team A.
- **R8** — this log, `docs/workflow.md`, `.github/CODEOWNERS`, `.github/pull_request_template.md`.

**Available tomorrow (Day 1 morning), for every team:**

| What | Where |
|---|---|
| Frozen contracts | `packages/contracts/docs/*.md` — read before writing any endpoint or record shape |
| The extracted engine | `packages/engine/` — Team C builds C1–C10 directly on top of this |
| Frozen fixtures | `src/mocks/*.json` — Team B's starting point; see `src/mocks/README.md` for which real
  endpoint retires which file |
| Dev views | `/dev/data`, `/dev/planner` (dev builds only) |
| Backend shell | `backend/server.js` + `backend/modules/{team-a,team-b,team-c}/` — add routes to your
  own module, nothing else to touch |

**Nothing to switch off yet** — Day 1 is the first day any team writes real feature code; the next
entry in this log is Night 1, recording what Team A and Team C ship for Team B to pick up on Day 2.
