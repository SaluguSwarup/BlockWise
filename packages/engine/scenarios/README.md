# Team C's synthetic scenario library

Empty as of Pre-Day-1. Team C fills this in on Day 1 (`C1` in `BlockWise.md`): small, hand-controlled
scenarios built to the `PlanningInput` contract (`packages/contracts/docs/planning.md`), covering at
minimum one basic case and one where three departments want the same section on the same night.
